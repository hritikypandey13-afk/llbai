export const metadata = {
  title: "LLBAI",
  description: "Unlock The World Of Law",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
